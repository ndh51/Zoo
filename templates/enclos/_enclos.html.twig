{% for enclo in enclos %}
    <div class="list-group" id="enclos">

        <a class="list-group-item list-group-item-action" href="{{ path('app_enclos_id', {id: enclo.id}) }}">{{ enclo.nomEnclos }}</a>

    </div>
{% endfor %}