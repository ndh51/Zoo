{% block stylesheets %}
    <link href="{{ asset('/public/css/styleEvenement.css') }}" rel="stylesheet" type="text/css" />

{% endblock %}

<div class="gauche">
    <div class="imgdesc">
        <img src="{{ asset('/public/img/FLOP.png') }}" alt="aie ptit flop">
        <p>description : {{ evenement.descEvent }}</p>
    </div>
</div>

<div class="droite">
    <div class="titre">
            <h1>{{ evenement.nomEvent}}</h1>
    </div>
    <div class="description">
            <p>nom évènement : {{ evenement.nomEvent}}</p>
            <p>enclos : {{ evenement.idEnclos.nomEnclos}}</p>
            <p>nombre de places maximums : {{ evenement.nbPlaceMaxEvent }}</p>
    </div>
    <div class="animaux">


    </div>

</div>
