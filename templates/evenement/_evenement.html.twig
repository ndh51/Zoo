<ul class="list-group" id="evenements">
    {% if is_granted('ROLE_ADMIN') %}
    <li class="list-group-item"><a href="{{ path('app_evenement_create') }}">Ajouter un évenement</a></li>
    {% endif %}
    {% for evenement in evenements %}
        <li class="list-group-item list-group-item-action">
            <a href="{{ path('app_evenement_show', {id: evenement.id}) }}">{{ evenement.nomEvent }}</a>
            {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('app_evenement_update',{id: evenement.id}) }}"><span class="material-symbols-outlined text-warning">edit</span></a><a href="{{ path('app_evenement_delete',{id: evenement.id}) }}"><span class="material-symbols-outlined text-danger">delete</span></a>
            {% endif %}
        </li>
    {% endfor %}
</ul>