{% for evenement in evenements %}
    <div class="list-group">

        <a class="list-group-item list-group-item-action" href="{{ path('app_evenement_id', {id: evenement.id}) }}">{{ evenement.nomEvent }}</a>

    <div>
{% endfor %}