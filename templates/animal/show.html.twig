{% extends 'base.html.twig' %}

{% block title %}Détails de {{ animal.nomAnimal }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="/css/styleAnimal.css">
{% endblock %}

{% block body %}

    <div class="animal">
        <div class="corps-animal">
            <img class="animalPresenter" src="{{ asset(animal.image.pathImage) }}" alt="image {{ animal.nomAnimal }}">
            <div class="infoAnimal">
                <p class="nomAnimal">{{ animal.nomAnimal|first|upper}}{{ animal.nomAnimal|slice(1) }}</p>
                <div>
                    <p class="titre">Emplacement </p>
                    <p> {{ animal.idEnclos.nomEnclos|first|upper}}{{ animal.idEnclos.nomEnclos|slice(1) }}</p>
                </div>
                <div>
                    <p class="titre">Famille</p>
                    <p> {{ animal.idFamille.nomFamille|first|upper}}{{ animal.idFamille.nomFamille|slice(1) }} </p>
                </div>
                <div>
                    <p class="titre">Catégorie</p>
                    <p>{{ animal.idCategorie.nomCategorie|first|upper}}{{ animal.idCategorie.nomCategorie|slice(1) }}</p>
                </div>
            </div>
        </div>
        <p class="titre">Description</p>
        <p>{{ animal.descAnimal|first|upper}}{{ animal.descAnimal|slice(1) }}</p>
    </div>
    <div class="AnimauxetEvenements">
        {% if sameFamille is not empty %}
        <div class="animalFamille">
            <p class="titre">Animaux de la même famille</p>
            {% for animal in sameFamille %}
            <div class="animalFamilleImg">
                <a href="{{ path('app_animal_id', {id: animal.id}) }}">
                <img src="{{ asset(animal.idImage.pathImage) }}" alt="image {{ animal.nomAnimal|first|upper}}{{ animal.nomAnimal|slice(1) }}">
                <p>{{ animal.nomAnimal|first|upper}}{{ animal.nomAnimal|slice(1) }}</p>
                </a>
            </div>
            {% endfor %}
        </div>
        {% endif %}
        {% if events is not empty %}
        <div class="evenementAnimal">
            <p class="titre">Évènements avec {{ animal.nomAnimal }}</p>
            {% for evenement in events %}
            <a href="{{ path('app_evenement_id', {id: evenement.id}) }}">
            <img src="{{ asset(evenement.idImage.pathImage) }}" alt="image {{ evenement.nomEvent }}">
                <p>{{ evenement.nomEvent|first|upper}}{{ evenement.nomEvent|slice(1) }}</p>
            </a>
            {% endfor %}
        </div>
        {% endif %}
    </div>

{% endblock %}
