{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}
{% block body %}

    <title>Édition de {{ animal.nomAnimal}}</title>
    <h1>Édition de {{ animal.nomAnimal }}</h1>

    {{ include('animal/_form.html.twig', {'form': form, 'submit_label' : 'Modifier'}, with_context = false) }}
{% endblock %}

