{{ form_start(form, {'method': 'POST'}) }}
    {{form_row(form.nomAnimal)}}
    {{ form_row(form.descAnimal)}}
    {{ form_row(form.idFamille)}}
    {{ form_row(form.idCategorie)}}
<button type="submit" class="btn btn-success">{{ submit_label }}</button>
{{ form_end(form) }}