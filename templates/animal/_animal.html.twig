{% for animal in animaux %}
    <div class="list-group" id="animals">

        <a class="list-group-item list-group-item-action" href="{{ path('app_animal_id', {id: animal.id}) }}">{{ animal.nomAnimal }}</a>

    </div>
{% endfor %}