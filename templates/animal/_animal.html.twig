{% for animal in animaux %}
    <div class="list-group">

        <a class="list-group-item list-group-item-action" href="{{ path('app_animal_show', {id: animal.id}) }}">{{ animal.nomAnimal }}</a>

    </div>
{% endfor %}