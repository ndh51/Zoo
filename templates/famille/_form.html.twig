{{ form_start(form, {'method': 'POST'}) }}
    {{ form_row(form.nomFamille)}}
    {{ form_row(form.descFamille)}}
<button type="submit" class="btn btn-success">{{ sumbmit_label }}</button>
{{ form_end(form) }}
